---
title: Release Checksums
layer: project-governance
status: stable
version: 0.2.7
updated: 2026-08-16
---

# Release Checksums

## v0.2.7 发布检查

- Markdown 文件：183 个。
- 所有 Markdown 均可按 UTF-8 读取并包含 front matter、`version`、`updated` 与合法 `status` 字段。
- 已检查 761 个项目内相对 Markdown 链接，无断链。
- v0.2.7 的方向确认由 `D-025`、`PCR-A2D7F9`、`SRC-A2D7F914`、两份外部文件哈希、单独确认记录和 Source Registry 互相链接。
- 《有限互惠公共责任机制》及积分、质量和专业参考机制保持 `draft`；方向接受不等于年度时数、系数、算法、权益清单或后果已经验证。
- 语义回归确认 `D-025` 细化 `D-024`：允许年度责任积分、领域累计贡献和附带构成的总量入口，但不恢复终身责任债务、人格评分、基本保障绑定、跨领域权威或自动派工。
- 任务责任义务仍须先经必要性、机构先行责任、自愿不足证明、公平分配、能力与条件、豁免或替代及申诉程序成立；账户未达标不单独自动派工。
- `draft`、`question`、`confirmed`、`archive` 状态保持分离；方向接受未被写成现实验证完成。
- 原始外部聊天、Markdown 和 RTF 只登记最小来源标识与哈希，不进入项目 ZIP。
- ZIP 不包含 `.DS_Store`、AppleDouble `._*`、旧版 ZIP 或原始外部聊天/RTF。
- 所有非 ASCII ZIP 路径使用 UTF-8 标记。
- ZIP CRC 与解压后逐文件内容在发布时复核。

最终 ZIP 的 SHA-256 在交付消息中提供，避免压缩包把自身哈希写入内部文件造成循环变化。
